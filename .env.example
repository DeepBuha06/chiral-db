# Database Configuration (Shared Defaults)
# Copy this file to .env to get started

# PostgreSQL
POSTGRES_USER=admin
POSTGRES_PASSWORD=dev_password_only
POSTGRES_DB=chiral_db
POSTGRES_PORT=5432

# MongoDB
MONGO_INITDB_ROOT_USERNAME=admin
MONGO_INITDB_ROOT_PASSWORD=dev_password_only
MONGO_PORT=27017
